spring.application.name=server
server.address=0.0.0.0
server.port=8080

# LAN hosting requires four players. The desktop Practice button explicitly
# enables testing mode on a separate loopback-only server process.
game.testing-mode=false
game.allowed-origins=http://localhost:8080
