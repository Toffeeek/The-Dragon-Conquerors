package com.github.thedragonconquerors.animation;

/** Each state selects a separate strip; frame counts belong to the character profile. */
public enum AnimationState { IDLE, WALK, ATTACK, CAST, SPECIAL, HURT, DEATH }
