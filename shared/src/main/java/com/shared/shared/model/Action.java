package com.shared.shared.model;

public enum Action
{
    JOIN,
    PRIVATE_JOIN_CONFIRMATION,
    PRIMARY,
    SECONDARY,
    ULTIMATE,
    MOVE,
    LEAVE,
    ERROR,
    PLAYER_COORDINATE,
    EOF
}
